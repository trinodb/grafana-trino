# Grafana's plugin tooling resolves modules through a real node_modules tree:
# webpack, jest and playwright all read from disk. Yarn defaults to Plug'n'Play,
# which has no node_modules, so the linker has to be set back explicitly.
nodeLinker: node-modules
